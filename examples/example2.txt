record Paradosso: id: int;
define Paradosso where Paradosso.id == 1;

record Evento: id: int;

/* Un Evento accade solo se se stesso NON accade */
define Evento as e1
    from Paradosso as p, not Evento as e2
    where e1.id == p.id and e2.id == p.id;

show Evento;